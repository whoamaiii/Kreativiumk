import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kreativium.neurologg',
  appName: 'Kreativiumtest',
  webDir: 'dist'
};

export default config;
